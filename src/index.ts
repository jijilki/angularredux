//Importing Reducers
/*
import {catReducer,workoutReducer} from './store';
import {StoreModule} from '@ngrx/store';

//Importing states
import {ICatState,IWorkOState } from './store';
import {INITIAL_STATE_CAT,INITIAL_STATE_WO } from './store';
export interface IWorkoutTrackerState {
    catState:ICatState,
    woState:IWorkOState
}



export interface IState {
    userModule: IWorkoutTrackerState;    
}

export const reducers = {
    catRed:catReducer,
    woRed:workoutReducer
}

//export const selectUserModuleState = createFeatureSelector<IWorkoutTrackerState>('workoutTrackerModule'); */